package com.cctv.edge_ai_cctv

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity is required by local_auth (biometric prompt).
// The "Loss prevention" notification channel is created from Dart
// (NotificationService) as a normal high-importance channel.
class MainActivity : FlutterFragmentActivity()
