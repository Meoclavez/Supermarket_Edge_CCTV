fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.4
pydantic-settings>=2.2.1
sqlalchemy[asyncio]>=2.0.28
aiosqlite>=0.20.0
cryptography>=42.0.0
zeroconf>=0.132.0
python-multipart>=0.0.9
opencv-python-headless>=4.9.0.80
numpy>=1.26.4
aiofiles>=23.2.1
pyjwt>=2.8.0
httpx[http2]>=0.27.0
pytest>=8.1.1
pytest-asyncio>=0.23.6
# bcrypt is used directly. passlib is deliberately NOT a dependency: its
# backend probe hashes an over-length string, which bcrypt >= 4.1 rejects,
# breaking every password hash and verify. passlib is unmaintained since 2020.
bcrypt>=4.1.0
# Person detection. Install onnxruntime-gpu instead on a machine with a
# dedicated GPU; the backend probes providers at runtime and will pick up
# TensorRT/CUDA automatically without any code change.
onnxruntime>=1.17.0
